# Mars Mission

## How to run

  From the root directory

  - Install dependencies:

    ```sh
    $ yarn
    # or
    $ npm install
    ```

  - Run the app:

    ```sh
    yarn start
    # or
    $ npm start
    ```

  - Visit `http://localhost:3000/`

  - Enjoy! (hopefully)
